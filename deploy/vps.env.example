# Copie para deploy/vps.local.env (NÃO commitar — está no .gitignore)
# Usado pelo agente Cursor para SSH na VPS em manutenções futuras.

VPS_HOST=161.97.79.7
VPS_USER=root
VPS_APP_DIR=/opt/legendas-locais
VPS_DOMAIN=legendas.clonefyia.com
GITHUB_REPO=git@github.com:sellpayclub/gerador-legendas.git

# Acesso: preferir chave SSH (~/.ssh/id_ed25519).
# Senha root NÃO deve ficar neste arquivo nem no git.
